
        if (list1 == null) return list2;
        if (list2 == null) return list1;
        
        if (list1.val <= list2.val) {
            list1.next = mergeTwoLists(list1.next, list2);
            return list1;
        } else {
            list2.next = mergeTwoLists(list1, list2.next);
            return list2;
        }

        
🔎 Example Walkthrough

Input:
list1 = [1, 3, 5]
list2 = [2, 4, 6]

Steps:

Compare 1 and 2 → 1 is smaller.
1 → mergeTwoLists([3,5], [2,4,6])

Compare 3 and 2 → 2 is smaller.
1 → 2 → mergeTwoLists([3,5], [4,6])

Compare 3 and 4 → 3 is smaller.
1 → 2 → 3 → mergeTwoLists([5], [4,6])

Compare 5 and 4 → 4 is smaller.
1 → 2 → 3 → 4 → mergeTwoLists([5], [6])

Compare 5 and 6 → 5 is smaller.
1 → 2 → 3 → 4 → 5 → mergeTwoLists([], [6])

Base Case: list1 is empty → return list2.

Final merged list:
1 → 2 → 3 → 4 → 5 → 6

⚡ Key Takeaways

Recursive solutions are often shorter and elegant, but can use more stack memory.

Always identify base case + recursive case.

Trust recursion: don’t simulate everything, just reduce the problem.
